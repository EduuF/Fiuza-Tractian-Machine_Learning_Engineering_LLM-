version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/ChromaDB/DB_instance:/app/backend/ChromaDB/DB_instance
      - ./.hf_cache:/root/.cache/huggingface

    env_file:
      - .env